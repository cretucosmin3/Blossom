using System.Drawing;

namespace Blossom.Utils
{
    internal static class Conversion
    {
        // internal static Colour fromColor(Color color)
        // {
        //     return new Colour(color.R, color.G, color.B, color.A);
        // }

        // internal static Color toColor(Colour color)
        // {
        //     return Color.FromArgb(
        //         (int)(255 * color.A),
        //         (int)(255 * color.R),
        //         (int)(255 * color.G),
        //         (int)(255 * color.B)
        //     );
        // }
    }
}
